/* 
	built-in 객체(함수)
	ECMA Script에 정의된 객체로,
	어떤 환경에서든 똑같이 작동하는 표준 객체
	(표준에 정의된 것만 40여가지)

	host 객체(함수)라는 것도 있는데, 
	ECMA에 정의되어 있지 않은 특정 환경에서만 작동하는 객체를 의미
*/
// alert('!') // 브라우저 내에서만 작동하므로(node 환경에서는 작동 X), host
console.log('😀')   // 어떤 환경에서도 작동하므로 built-in